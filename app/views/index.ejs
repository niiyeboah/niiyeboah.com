<!DOCTYPE html>
<html>
<head>
    <title><%=title %></title>
    <meta name="viewport" content="width=device-width">
    <script src="js/svg.js/dist/svg.min.js"></script>
    <script src="js/app.js"></script>
    <link rel="shortcut icon" href="img/favicon.ico" type="image/x-icon">
    <link rel="icon" href="img/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="css/pikaday.css" type="text/css">
    <style>
        body {
            height: 100vh;
            padding:0 ; 
            margin: 0; 
            background-image: radial-gradient(farthest-corner at 0 0, #FFF 50%, #777 140%);
        }
        #container { width: 408px; padding: 8px; }
        #logo { display: block; float: left; }
        #drawing { border: 4px solid #000; width: 400px; height: 400px; cursor: pointer; }
        #drawing:hover { box-shadow: 0 0 60px 0 #396; }
        #top { margin-bottom: 8px; clear: both; height: 32px; user-select: none; }
        #date { 
            display: block; 
            float: right; 
            margin: 0; 
            height: 32px; 
            border: none; 
            padding: 0; 
            width: 215px; 
            padding: 0; 
            background: transparent;  
            text-align: right; 
            font-size: 32px; 
            font-family: Verdana, Geneva, Tahoma, sans-serif; 
            font-weight: bold;
        }
        #date:hover { color: #396; }

        @media only screen and (max-width: 768px) {
            #container { width: 98%; padding: 8px 1%; }
            #drawing { border: none; width: 98%; height: 98%; margin-bottom: 1%; }
            #drawing_container { padding: 2% 0 0 1.5%; background: #000; height: 100%; width: 98% }
        }
    </style>
</head>
<body>
    <div id="container">
        <div id="top">
            <img id="logo" src="img/logo.png" alt="Logo" />
            <input id="date" type="text" value="<%=date %>" data-curr="<%=date %>" />
        </div>
        <div id="drawing_container">
            <div id="drawing"></div>
        </div>
    </div>
</body>
</html>